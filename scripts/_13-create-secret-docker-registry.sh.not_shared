#!/bin/bash
kubectl create secret docker-registry docker-cred \
  --docker-server=https://index.docker.io/v1/ \
  --docker-username=smachida \
  --docker-password=Prtn2501Prtn2501 \
  --docker-email=shuichi.machida@gmail.com
